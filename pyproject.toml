[project]
name = "localai"
version = "0.1.0"
description = "Local AI service with multi-agent harness, RAG, search, and messenger connectors"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I"]
